# Copy to .env and adjust. .env is gitignored.

# Backend API base URL when different from same origin (e.g. dev backend on another port)
# VITE_API_URL=http://localhost:8000

# Set to true to skip login and access all routes (for working on UIs). API calls that need auth will still 401.
VITE_DISABLE_AUTH=true
