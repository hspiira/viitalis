/**
 * Shared form input and label class names. Use for CRUD dialogs and forms
 * so styles stay consistent and we avoid repeating long Tailwind strings.
 */

export const FORM_INPUT_CLASS =
  'w-full border border-[var(--input)] bg-[var(--secondary)] px-3 py-2 text-sm text-[var(--foreground)] rounded-md focus:outline-none focus:ring-2 focus:ring-[var(--ring)]'

export const FORM_LABEL_CLASS =
  'block text-sm font-medium text-[var(--foreground-muted)] mb-1.5'
